import 'package:flutter/material.dart';

class KeyDerivationScreen extends StatelessWidget {
  const KeyDerivationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Key Derivation'));
  }
}
