import 'package:flutter/material.dart';

class HashScreen extends StatelessWidget {
  const HashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Hash"));
  }
}
