import 'package:flutter/material.dart';

class EncodingScreen extends StatelessWidget {
  const EncodingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Encoding"));
  }
}
