import 'package:flutter/material.dart';

class HMACScreen extends StatelessWidget {
  const HMACScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('HMAC'));
  }
}
