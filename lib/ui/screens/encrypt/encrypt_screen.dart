import 'package:flutter/material.dart';

class EncryptScreen extends StatelessWidget {
  const EncryptScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Encrypt/Decrypt'));
  }
}
