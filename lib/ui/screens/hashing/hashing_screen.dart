import 'package:flutter/material.dart';

class HashingScreen extends StatelessWidget {
  const HashingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Hashing"));
  }
}
